<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MainActivityTheme" parent="@android:style/Animation.Activity">
        <!--         <item name="android:activityOpenEnterAnimation">@anim/push_bottom_in</item> -->
        <!--         <item name="android:activityOpenExitAnimation">@anim/push_bottom_out</item> -->
        <item name="android:activityCloseEnterAnimation">@anim/push_top_in</item>
        <item name="android:activityCloseExitAnimation">@anim/push_top_out</item>
    </style>

    <style name="PoetryActivityTheme" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/push_bottom_in</item>
        <item name="android:activityOpenExitAnimation">@anim/push_bottom_out</item>
        <!-- 小米手机把下面两行去掉 -->
        <item name="android:activityCloseEnterAnimation">@anim/push_left_in</item>
        <item name="android:activityCloseExitAnimation">@anim/push_left_out</item>
    </style>

    <style name="FavourActivityTheme" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/push_right_in</item>
        <item name="android:activityOpenExitAnimation">@anim/push_right_out</item>
        <!--         <item name="android:activityCloseEnterAnimation">@anim/push_left_in</item> -->
        <!--         <item name="android:activityCloseExitAnimation">@anim/push_left_out</item> -->
    </style>

    <style name="MyDialog" parent="@android:Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>
